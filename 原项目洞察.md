# 原项目洞察

本文中的“原项目”指 `~/CLionProjects/yacreader` 中的 YACReader 阅读器模块。阅读范围以 `YACReader/` 为主，同时包含它通过工程文件直接编入阅读器进程的 `common/`、`compressed_archive/`、`custom_widgets/`、`shortcuts_management/` 等共享模块。

## 1. 框架与依赖

原项目阅读器是一个 C++17 + Qt Widgets 桌面应用。构建系统使用 qmake：根工程是 `YACReader.pro`，阅读器子工程是 `YACReader/YACReader.pro`。`config.pri` 明确要求 Qt 5.15 或更新版本，并启用 C++17。

阅读器核心 Qt 模块包括：

- `Qt Widgets`：主窗口、阅读区、工具栏、对话框和各种覆盖控件。
- `Qt Core`：线程、信号槽、设置、文件路径、容器等基础能力。
- `Qt Network`：版本检查、翻译等网络请求。
- `Qt Multimedia`：翻译/朗读相关能力中使用 `QMediaPlayer`。
- `Qt SVG`：图标和 SVG 资源支持。
- Qt 6 下额外使用 `openglwidgets` 与 `core5compat`。

图像与渲染主要依赖 Qt 自身的 `QImage`、`QPixmap`、`QImageReader`、`QPainter`。底部 Go To Flow 缩略图导航在 OpenGL 可用时使用 `QOpenGLWidget`、`QOpenGLTexture` 等实现更流畅的封面流效果；在 ARM 或配置禁用 OpenGL 时会退回普通 Qt Widgets 实现。

压缩包后端是可切换的。`config.pri` 中的默认策略是：Linux 默认 `unarr`，macOS 和 Windows 默认 `7zip`，也可配置 `libarchive`。阅读器通过 `compressed_archive/wrapper.pri`、`compressed_archive/unarr/unarr-wrapper.pri` 或 `compressed_archive/libarchive/libarchive-wrapper.pri` 接入具体后端。支持 ZIP/CBZ、RAR/CBR、7z、tar 等格式的能力主要来自这些后端。

PDF 后端也按平台切换：Windows 默认 `pdfium`，Linux 默认 `poppler`，macOS 默认 `PDFKit`。相关选择在 `dependencies/pdf_backend.pri` 和 `config.pri` 中完成。

其他重要依赖与模块包括：

- `QsLog`：日志库，通过 `third_party/QsLog/QsLog.pri` 编入。
- `shortcuts_management`：快捷键注册、编辑与持久化。
- `custom_widgets`：跨平台自定义控件，macOS 下有 Objective-C++ 工具栏实现。
- `QLocalSocket`：阅读器与 YACReaderLibrary 之间的本地 IPC。
- `QSettings`：通过 `YACReader.ini` 持久化阅读器配置。

阅读器本身不是一个数据库驱动的页面加载程序。书库侧 SQLite/QSql 的主要实现位于 `YACReaderLibrary/`，阅读器模块更多是消费漫画路径、元数据和本地 IPC 信息。

## 2. 高性能实现方式

原项目打开 100MB 漫画 ZIP 仍然流畅，关键不在于把所有东西一次性加载完，而在于把“归档读取、页面原始字节、图片解码、UI 显示”拆开，并用后台线程、当前页优先和小窗口缓存组织起来。

### 2.1 压缩包按条目读取

原项目不会先把整个 ZIP/CBZ 读成一个大内存块再处理。压缩包封装层先打开归档、扫描条目名和偏移/索引，然后按页面条目读取。

在 `unarr` 实现中，构造 `CompressedArchive` 时只解析条目，记录 `fileNames` 和 `offsets`；真正取页面时才通过 `ar_parse_entry_at` 定位条目，再用 `ar_entry_uncompress` 解压这一页。

在 7zip 实现中，构造阶段打开 `IInArchive`，记录压缩包内文件索引；后续通过 `Extract` 按索引提取页面数据。两条路径都说明它的归档层是“按条目工作”，不是“整包一次性展开后再显示”。

### 2.2 漫画处理在线程中执行

`Render::startLoad()` 会创建 `QThread`，把 `Comic` 对象 `moveToThread` 到后台线程，然后在线程启动时调用 `Comic::process()`。主 UI 线程只通过信号槽接收页数、当前页、某页图片原始数据已加载等事件。

这意味着打开大文件时，耗时的归档扫描和页面解压不会长时间阻塞窗口事件循环。用户看到的是“页面逐步可用”，而不是主界面卡死等待全书处理完成。

### 2.3 先公布页数和起始页，再逐页发信号

`FileComic::process()` 的流程是：

1. 打开 `CompressedArchive`。
2. 获取归档内文件名，过滤出支持的图片文件。
3. 为 `_pages` 和 `_loadedPages` 分配与页数对应的容器。
4. 立即发送 `numPages` 和 `openAt(_index)`，让 UI 知道总页数和应该打开的位置。
5. 后台继续解压页面。
6. 每解压完一页，`FileComic::fileExtracted()` 就写入 `_pages[sortedIndex]` 并发送 `imageLoaded`。

这是一种渐进式可用的设计。首屏或上次阅读页不需要等所有页面都解压完成。

### 2.4 解压顺序围绕当前页优化

原项目不是简单从压缩包第一个条目一路解压到最后。它会先把自然排序后的阅读顺序映射回压缩包物理条目索引，然后用 `FileComic::getSections()` 切分成多个物理顺序单调的 section。

之后 `FileComic::process()` 会从包含 `_firstPage` 的 section 开始解压，先处理当前页所在段及之后的段，再处理之前的段。这样打开书签页或恢复上次阅读位置时，当前阅读区域更快可用，同时尽量保持对压缩包条目的顺序访问，减少随机跳转成本。

### 2.5 原始字节与解码后的图片分层缓存

原项目会把已解压页面的原始图片字节放进 `Comic::_pages`，也就是 `QVector<QByteArray>`。这是“压缩包条目解压后的 JPEG/PNG/WebP 等原始文件字节”，不是解码后的像素大图。

真正占 CPU 和显存/内存的图片解码由 `PageRender` 线程完成：`PageRender::run()` 中调用 `QImage::loadFromData()`，再执行旋转、亮度、对比度、Gamma 等滤镜，最后把结果写入 `Render` 的页面缓冲。

这种分层很重要：原始字节可以全书逐步累积，而解码后的大图只围绕当前页保留一个小窗口，避免一次性解码整本漫画。

### 2.6 固定窗口预解码

`Render` 构造时设置 `numLeftPages = 4`、`numRightPages = 4`，加上当前页，总共维护 9 个页面槽位。翻页时 `updateBuffer()` 会滑动这个窗口：移除远离当前页的 `QImage` 和对应 `PageRender`，在新方向补空槽。

当某页原始数据到达时，`Render::pageRawDataReady()` 会标记 `pagesReady[page] = true`。如果这页在当前页左右窗口范围内，`fillBuffer()` 就会启动 `PageRender` 后台解码。也就是说，用户还没翻到的邻近页面已经在后台准备，翻页时往往只需要拿现成的 `QImage/QPixmap` 显示。

这就是原项目流畅翻页的核心：压缩包条目按需/渐进解压，图片解码只做当前页附近，且提前做。

### 2.7 缩略图导航单独限流

底部 Go To Flow 缩略图没有一次性生成全书缩略图。`GoToFlow::preload()` 用定时器节流，`updateImageData()` 只尝试中心页左右各 8 张左右的可见邻近页面。缩略图生成由 `PageLoader` 线程执行：加载原始字节、缩放到小尺寸，再交给 flow 控件。

这避免了“打开大书时同时解码几百张缩略图”的灾难，也避免缩略图任务抢占主阅读页面的 CPU。

### 2.8 需要注意的内存取舍

原项目并不是极致低内存架构。它最终会把全书所有页面的原始图片字节逐步放入 `_pages`。它快，是因为：

- 压缩包不是整包一次性读入。
- 解压和解码都不阻塞 UI 线程。
- 当前页附近优先。
- 解码后的大图只维护固定小窗口。
- 缩略图生成被限制在可见范围附近。

因此 Rust 重构可以直接借鉴它的调度模型，但可以进一步改进原始字节缓存，例如引入有界 LRU、磁盘临时缓存或 mmap，避免超大漫画最终占用过多内存。

## 3. UI 设计方式

原项目阅读器 UI 是经典 Qt Widgets 桌面应用设计：`QMainWindow` 负责菜单、工具栏、动作和窗口状态；中央控件是自定义阅读视图 `Viewer`；阅读视图内部再叠加缩略图条、页码信息、通知、放大镜、翻译面板等辅助控件。

### 3.1 主窗口负责动作和应用框架

`MainWindowViewer` 继承 `QMainWindow`。`setupUI()` 中创建 `Viewer` 并 `setCentralWidget(viewer)`，随后创建 actions、快捷键、工具栏、选项对话框、帮助对话框等。

大多数用户操作先表现为 `QAction`：打开文件、打开文件夹、翻页、适应宽度/高度/整页/原始尺寸、双页模式、日漫双页顺序、全屏、显示工具栏、旋转、放大镜、选项等。快捷键通过 `ShortcutsManager` 注册和管理，用户可以在快捷键编辑对话框中调整。

这种设计把“应用命令”集中在主窗口中，便于菜单、工具栏、快捷键共享同一套动作。

### 3.2 阅读区是滚动区域中的图片控件

`Viewer` 继承 `QScrollArea`，内部用一个 `QLabel *content` 作为 `setWidget(content)`。当前页最终以 pixmap 的形式放到这个 label 上。滚动条策略设置为 AlwaysOff，但内部仍然使用滚动条位置来实现拖拽平移、滚轮滚动和动画滚动。

阅读区本身负责：

- 当前页显示与尺寸适配。
- 滚轮与触控板事件。
- 鼠标拖拽平移。
- 双页/日漫双页状态。
- Go To Flow 显示隐藏动画。
- 背景色更新。
- 放大镜、页码信息、通知等覆盖层。

这种方式简单直接：页面显示不是复杂 scene graph，而是在 Qt Widgets 里围绕一个滚动视图组织。

### 3.3 交互模式丰富但集中

鼠标交互由 `MouseHandler` 集中处理。主要模式包括：

- Normal：常规模式，左键拖拽平移，双击可切换全屏。
- LeftRightNavigation：左键/右键翻页，短距离点击与拖拽通过阈值区分。
- HotAreas：按屏幕左右区域点击翻页。
- 鼠标侧键：前进/后退页。
- 鼠标移到底部区域：显示 Go To Flow 缩略图条。

滚轮逻辑在 `Viewer` 中区分鼠标滚轮和触控板 `pixelDelta`。当滚动到页面顶部或底部时，可根据配置触发上一页/下一页。这种设计让“滚动阅读”和“翻页阅读”可以共用同一个阅读区。

### 3.4 阅读模式通过配置单例持久化

`Configuration` 是全局配置单例，底层使用 `QSettings`。它保存适应模式、双页、日漫双页顺序、背景色、全屏、工具栏显示、鼠标模式、滚轮翻页策略、Go To Flow 类型、放大镜尺寸与倍率等。

窗口启动时读取 `YACReader.ini`，关闭或设置变更时写回。UI 状态不是散落在配置文件读写里，而是通过 `Configuration::getConfiguration()` 暴露统一访问器。

### 3.5 视觉风格偏系统原生

原项目没有独立 QML 或复杂主题系统。视觉上依赖 Qt/system style、少量 QSS、`QPalette` 和资源图标。非 macOS 工具栏使用少量内联 stylesheet；macOS 下有专门的系统风格工具栏实现。阅读区背景色可配置。

整体 UI 设计更重视桌面应用的稳定、快捷键完整、状态持久和阅读效率，而不是高度自绘的视觉系统。

## 4. 对 Rust 重构的启发

Rust 重构不应该复刻“全量加载模式”。更接近原项目性能表现的架构应当拆成以下层次：

1. `ArchiveIndex`：打开压缩包，只扫描条目名、大小、压缩包内索引/偏移，不读取所有页面内容。
2. `PageOrder`：对图片文件名做自然排序，并保留“阅读顺序 -> 压缩包物理条目”的映射。
3. `PageSource`：按页索引读取原始图片字节，支持取消和优先级。
4. `DecodeWorker`：后台解码当前页和邻近页，输出 UI 可显示的 bitmap/texture。
5. `DisplayCache`：只缓存当前页左右固定窗口的已解码图。
6. `ThumbnailQueue`：低优先级、限流、只围绕可见范围生成缩略图。

调度策略建议：

- 打开文件后先返回页数和起始页，不等待全书解压。
- 以当前页所在 section 为优先级，从当前页附近向后、再向前加载。
- 解压和解码都放到后台线程或阻塞任务池。
- 解码任务必须有并发上限，避免对大书一次性启动大量任务。
- UI 线程只消费“页面原始字节已就绪”“页面解码已就绪”“加载完成/失败”等事件。
- 缩略图任务和主阅读任务分开队列，主阅读优先级更高。
- 原始字节缓存最好做成有界缓存，而不是无上限保存全书。

如果本项目目标是超越原项目的大文件表现，重点不只是“按需解压”，还应控制全书 raw bytes 的长期占用。原项目的体验已经证明：固定窗口解码 + 后台渐进加载足以带来流畅交互；Rust 版本可以在此基础上增加更严格的内存预算。

## 5. 关键源码索引

- `YACReader/YACReader.pro`：阅读器子工程、Qt 模块、源码列表、压缩后端、PDF 后端、日志和资源配置。
- `config.pri`：C++17、Qt 最低版本、OpenGL 开关、压缩后端默认值、PDF 后端默认值。
- `dependencies/pdf_backend.pri`：pdfium、poppler、PDFKit 的平台选择。
- `compressed_archive/unarr/compressed_archive.cpp`：unarr 后端，按条目 offset 解压。
- `compressed_archive/compressed_archive.cpp`：7zip 后端，按 archive item index 提取。
- `common/comic.cpp`：`FileComic::process()`、`FileComic::getSections()`、`FileComic::fileExtracted()`，是渐进式加载和当前页优先的核心。
- `YACReader/render.h` / `YACReader/render.cpp`：`Render`、`PageRender`、页面解码线程、固定窗口缓存、预取逻辑。
- `YACReader/viewer.h` / `YACReader/viewer.cpp`：阅读区控件、滚动、适配、覆盖层、Go To Flow 装配。
- `YACReader/main_window_viewer.h` / `YACReader/main_window_viewer.cpp`：主窗口、工具栏、菜单、快捷键、全屏和动作系统。
- `YACReader/mouse_handler.cpp`：鼠标拖拽、热区、左右键模式、底部缩略图悬停显示。
- `YACReader/goto_flow.cpp` / `YACReader/goto_flow_gl.h`：底部缩略图导航、缩略图限流生成、OpenGL 版本入口。
- `YACReader/configuration.h` / `YACReader/configuration.cpp`：阅读器配置单例与持久化。
